<?xml version="1.0" encoding="utf-8"?>
<!-- Intentionally empty: the full image lives on the adaptive-icon <background>
     layer, which spans the entire 108dp canvas edge-to-edge (no safe-zone
     scaling -> "no zoom"). Foreground must exist but stays transparent. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M0,0h108v108h-108z"
        android:fillColor="#00000000" />
</vector>